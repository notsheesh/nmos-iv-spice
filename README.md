# NMOS SPICE
## Setup 
```
# Clone this repo in your workspace
git clone https://github.com/notsheesh/nmos-spice.git

# Rename the repository to the library name 'NMOS'
cd ../ && mv nmos-spice NMOS

# Start Cadence Virtuoso and open the NMOS library 
File > Open > Library > NMOS
